<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="myview.zz.com.music.MainActivity">

    <Button
        android:text="startMusic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_marginTop="21dp"
        android:id="@+id/startMusic"
        android:onClick="start"/>

    <Button
        android:text="stopMusic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/startMusic"
        android:layout_centerHorizontal="true"
        android:id="@+id/stopMusic"
        android:onClick="stop"/>

    <Button
        android:text="pause"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/startMusic"
        android:layout_alignParentStart="true"
        android:layout_marginTop="13dp"
        android:onClick="pause"
        android:id="@+id/pause"
        android:layout_alignEnd="@+id/startMusic" />

    <Button
        android:text="restart"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/pause"
        android:layout_alignStart="@+id/stopMusic"
        android:onClick="restart"
        android:id="@+id/restart"
        android:layout_alignEnd="@+id/stopMusic" />
</RelativeLayout>
